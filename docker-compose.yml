services:
  dutch-daily:
    build: .
    image: dutch-daily:latest
    container_name: dutch-daily
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Europe/Amsterdam
    volumes:
      # state.json lives here so it survives container rebuilds
      - ./data:/app/data
      # saved preview .html files (also handy for checking before emailing)
      - ./output:/app/output
